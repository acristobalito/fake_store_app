class SectionParameterizationModel {
  String? title;
  String? category;
  String? titleColor;
  String? colorSection;

  SectionParameterizationModel(
      {this.title, this.category, this.titleColor, this.colorSection});
}
